openapi: 3.0.1
info:
  contact:
    email: support@sublime.security
  title: Multi-Tenancy API (BETA) BinExplode Message Groups API
  version: "1.0"
servers:
- url: '{scheme}://{server}'
  variables:
    scheme:
      default: https
      enum:
      - http
      - https
    server:
      default: platform.sublime.security
      description: Base URL of your Sublime deployment
security:
- bearerAuth: []
tags:
- name: Message Groups
paths:
  /v0/message-groups:
    get:
      description: List message groups
      operationId: listMessageGroups
      parameters:
      - description: Filters result to only message groups with the provided attachment name
        explode: true
        in: query
        name: attachment_name__is
        schema:
          description: Filters result to only message groups with the provided attachment name
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided attachment SHA256
        explode: true
        in: query
        name: attachment_sha256__is
        schema:
          description: Filters result to only message groups with the provided attachment SHA256
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided attack score verdict
        explode: true
        in: query
        name: attack_score_verdict__is
        schema:
          description: Filters result to only message groups with the provided attack score verdict
          items:
            enum:
            - unknown
            - likely_benign
            - suspicious
            - malicious
            - graymail
            - spam
            type: string
          type: array
        style: form
      - description: Filters result to only message groups that have flagged (ONLY) rules with the 'Attack surface reduction' tag
        in: query
        name: attack_surface_reduction__filter
        schema:
          description: Filters result to only message groups that have flagged (ONLY) rules with the 'Attack surface reduction' tag
          format: bool
          nullable: true
          type: string
      - description: Filters result to only message groups with the provided canonical ID
        explode: true
        in: query
        name: canonical_id__is
        schema:
          description: Filters result to only message groups with the provided canonical ID
          items:
            type: string
          type: array
        style: form
      - description: Inclusive start datetime filter, in UTC using the ISO 8601 format (e.g., '2021-03-14T15:09:26Z'). Only message groups that contain a message processed at or after this time will be returned.
        in: query
        name: created_at__gte
        schema:
          description: Inclusive start datetime filter, in UTC using the ISO 8601 format (e.g., '2021-03-14T15:09:26Z'). Only message groups that contain a message processed at or after this time will be returned.
          format: date-time
          nullable: true
          type: string
      - description: Exclusive end datetime filter, in UTC using the ISO 8601 format (e.g., '2021-05-04T15:09:26Z'). Only message groups that contain a message processed before this time will be returned.
        in: query
        name: created_at__lt
        schema:
          description: Exclusive end datetime filter, in UTC using the ISO 8601 format (e.g., '2021-05-04T15:09:26Z'). Only message groups that contain a message processed before this time will be returned.
          format: date-time
          nullable: true
          type: string
      - description: 'DEPRECATED: Use first_message_reported_at__gte'
        in: query
        name: first_message_reported_at[gte]
        schema:
          description: 'DEPRECATED: Use first_message_reported_at__gte'
          format: date-time
          nullable: true
          type: string
      - description: Filters result to only message groups with a message first reported at or after the provided time. Datetime must be in UTC using the ISO 8601 format (e.g., '2021-03-14T15:09:26Z').
        in: query
        name: first_message_reported_at__gte
        schema:
          description: Filters result to only message groups with a message first reported at or after the provided time. Datetime must be in UTC using the ISO 8601 format (e.g., '2021-03-14T15:09:26Z').
          format: date-time
          nullable: true
          type: string
      - description: Filters result to only message groups with at least one flagged message
        in: query
        name: flagged
        schema:
          description: Filters result to only message groups with at least one flagged message
          format: bool
          nullable: true
          type: boolean
      - description: Filters result to only message groups with the provided flagged rule ID
        explode: true
        in: query
        name: flagged_rule_id__is
        schema:
          description: Filters result to only message groups with the provided flagged rule ID
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided flagged rule severity
        explode: true
        in: query
        name: flagged_rule_severity__is
        schema:
          description: Filters result to only message groups with the provided flagged rule severity
          items:
            enum:
            - informational
            - low
            - medium
            - high
            - critical
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided historically flagged rule ID
        explode: true
        in: query
        name: historically_flagged_rule_id__is
        schema:
          description: Filters result to only message groups with the provided historically flagged rule ID
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided historically flagged rule severity
        explode: true
        in: query
        name: historically_flagged_rule_severity__is
        schema:
          description: Filters result to only message groups with the provided historically flagged rule severity
          items:
            enum:
            - informational
            - low
            - medium
            - high
            - critical
            type: string
          type: array
        style: form
      - description: 'DEPRECATED: Use created_at__gte instead'
        in: query
        name: last_message_created_at[gte]
        schema:
          description: 'DEPRECATED: Use created_at__gte instead'
          format: date-time
          nullable: true
          type: string
      - description: The maximum number of message groups to return. If the value exceeds the maximum, then the maximum value will be used.
        in: query
        name: limit
        schema:
          description: The maximum number of message groups to return. If the value exceeds the maximum, then the maximum value will be used.
          format: int32
          maximum: 500
          nullable: true
          type: integer
      - description: Filters result to only message groups with the provided mailbox email
        explode: true
        in: query
        name: mailbox_email__is
        schema:
          description: Filters result to only message groups with the provided mailbox email
          items:
            type: string
          type: array
        style: form
      - description: The (zero-based) offset of the message groups to return
        in: query
        name: offset
        schema:
          description: The (zero-based) offset of the message groups to return
          format: int32
          nullable: true
          type: integer
      - description: Filters result to only message groups with the provided recipient email
        explode: true
        in: query
        name: recipient_email__is
        schema:
          description: Filters result to only message groups with the provided recipient email
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided reporter
        explode: true
        in: query
        name: reported_as_phish_by__is
        schema:
          description: Filters result to only message groups with the provided reporter
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups which have or have not been reviewed
        in: query
        name: reviewed
        schema:
          description: Filters result to only message groups which have or have not been reviewed
          format: bool
          nullable: true
          type: boolean
      - description: Filters result to only message groups with the provided sender display name
        explode: true
        in: query
        name: sender_display_name__is
        schema:
          description: Filters result to only message groups with the provided sender display name
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided sender domain
        explode: true
        in: query
        name: sender_domain__is
        schema:
          description: Filters result to only message groups with the provided sender domain
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided sender email
        explode: true
        in: query
        name: sender_email__is
        schema:
          description: Filters result to only message groups with the provided sender email
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided spam status
        explode: true
        in: query
        name: spam__is
        schema:
          description: Filters result to only message groups with the provided spam status
          items:
            enum:
            - spam
            - not_spam
            - mixed
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with the provided subject
        explode: true
        in: query
        name: subject__is
        schema:
          description: Filters result to only message groups with the provided subject
          items:
            type: string
          type: array
        style: form
      - description: Filters result to only message groups with at least one reported message
        in: query
        name: user_reported
        schema:
          description: Filters result to only message groups with at least one reported message
          format: bool
          nullable: true
          type: boolean
      - description: Filters result to only message groups with the provided vendor
        explode: true
        in: query
        name: vendor_id__is
        schema:
          description: Filters result to only message groups with the provided vendor
          items:
            type: string
          type: array
        style: form
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesV0ListMessageGroupsResponse'
          description: OK
      summary: List message groups
      tags:
      - Message Groups
  /v0/message-groups/dismiss:
    post:
      description: Dismiss all messages in multiple groups, including future messages.
      operationId: dismissMultipleMessageGroups
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DismissMultipleMessageGroupsInput'
      responses:
        '200':
          description: OK
      summary: Dismiss multiple message groups
      tags:
      - Message Groups
  /v0/message-groups/hunt:
    post:
      deprecated: true
      description: 'DEPRECATED: Hunt using MQL to find message groups. Please use the POST /v0/hunt-jobs endpoint instead.'
      operationId: huntMessageGroups
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HuntMessageGroupsInput'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesTaskAccepted'
          description: OK
      summary: Hunt message groups
      tags:
      - Message Groups
  /v0/message-groups/hunt/{id}:
    get:
      deprecated: true
      description: 'DEPRECATED: Retrieve the results of a completed hunt. Please use the GET /v0/hunt-jobs/:id endpoint instead.'
      operationId: getHuntResults
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        schema:
          description: Task ID
          format: uuid
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesGetHuntResponse'
          description: OK
      summary: Get results of a hunt
      tags:
      - Message Groups
  /v0/message-groups/move-to-graymail:
    post:
      description: Move to Graymail all messages in multiple groups, including future messages.
      operationId: graymailMultipleMessageGroups
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GraymailMultipleMessageGroupsInput'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesTaskAccepted'
          description: Accepted
      summary: Graymail multiple message groups
      tags:
      - Message Groups
  /v0/message-groups/quarantine:
    post:
      description: Quarantine all messages in multiple groups, including future messages.
      operationId: quarantineMultipleMessageGroups
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuarantineMultipleMessageGroupsInput'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesTaskAccepted'
          description: Accepted
      summary: Quarantine multiple message groups
      tags:
      - Message Groups
  /v0/message-groups/restore:
    post:
      description: Restore all previously-trashed messages in multiple groups and turn off automatic trashing of future messages in the groups. "For an Office 365 message source, each message will be put back in the folder it was in before. For a Google Workspace message source, any applied labels (e.g. "Trash") will be removed from each message."
      operationId: restoreMultipleMessageGroups
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RestoreMultipleMessageGroupsInput'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesTaskAccepted'
          description: Accepted
      summary: Restore multiple message groups
      tags:
      - Message Groups
  /v0/message-groups/review:
    post:
      description: Review, classify and take actions on message groups
      operationId: reviewMessageGroups
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReviewMessageGroupsInput'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HandlersReviewMessagesResponse'
          description: Accepted
      summary: Review message groups
      tags:
      - Message Groups
  /v0/message-groups/search:
    get:
      description: Search all message groups
      operationId: searchMessageGroups
      parameters:
      - description: Searches every field (performs a case insensitive, OR search in all fields). Not compatible with other search fields.
        in: query
        name: any
        schema:
          description: Searches every field (performs a case insensitive, OR search in all fields). Not compatible with other search fields.
          nullable: true
          type: string
      - description: Search for messages containing an attachment MD5 match
        in: query
        name: attachment_md5
        schema:
          description: Search for messages containing an attachment MD5 match
          maxLength: 32
          minLength: 32
          nullable: true
          type: string
      - description: Search for messages containing an attachment SHA1 match
        in: query
        name: attachment_sha1
        schema:
          description: Search for messages containing an attachment SHA1 match
          maxLength: 40
          minLength: 40
          nullable: true
          type: string
      - description: Search for messages containing an attachment SHA256 match
        in: query
        name: attachment_sha256
        schema:
          description: Search for messages containing an attachment SHA256 match
          maxLength: 64
          minLength: 64
          nullable: true
          type: string
      - description: Inclusive start datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-03-14T15:09:26Z'). Only message groups with a message processed at or after this time will be returned.
        in: query
        name: created_at[gte]
        schema:
          description: Inclusive start datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-03-14T15:09:26Z'). Only message groups with a message processed at or after this time will be returned.
          format: date-time
          nullable: true
          type: string
      - description: Exclusive end datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-05-04T15:09:26Z'). Only message groups with a message processed before this time will be returned.
        in: query
        name: created_at[lt]
        schema:
          description: Exclusive end datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-05-04T15:09:26Z'). Only message groups with a message processed before this time will be returned.
          format: date-time
          nullable: true
          type: string
      - description: Deprecated, use created_at[lt]. Exclusive end datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-05-04T15:09:26Z'). Only message groups with a message processed before this time will be returned.
        in: query
        name: created_at[lte]
        schema:
          description: Deprecated, use created_at[lt]. Exclusive end datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-05-04T15:09:26Z'). Only message groups with a message processed before this time will be returned.
          format: date-time
          nullable: true
          type: string
      - description: Search in attachment filenames (case insensitive wildcard match)
        in: query
        name: file_name
        schema:
          description: Search in attachment filenames (case insensitive wildcard match)
          nullable: true
          type: string
      - description: Inclusive start datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-03-14T15:09:26Z'). Only message groups reported at or after this time will be returned.
        in: query
        name: first_reported_as_phish_at[gte]
        schema:
          description: Inclusive start datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-03-14T15:09:26Z'). Only message groups reported at or after this time will be returned.
          format: date-time
          nullable: true
          type: string
      - description: Exclusive end datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-05-04T15:09:26Z'). Only message groups reported before this time will be returned.
        in: query
        name: first_reported_as_phish_at[lt]
        schema:
          description: Exclusive end datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-05-04T15:09:26Z'). Only message groups reported before this time will be returned.
          format: date-time
          nullable: true
          type: string
      - description: 'DEPRECATED: Use ''sender'' instead. Search in the From field. (case insensitive wildcard match)'
        in: query
        name: from
        schema:
          description: 'DEPRECATED: Use ''sender'' instead. Search in the From field. (case insensitive wildcard match)'
          nullable: true
          type: string
      - description: The maximum number of message groups to return. If the value exceeds the maximum, then the maximum value will be used.
        in: query
        name: limit
        schema:
          description: The maximum number of message groups to return. If the value exceeds the maximum, then the maximum value will be used.
          format: int32
          maximum: 500
          nullable: true
          type: integer
      - description: Search for a mailbox by email address (case insensitive, wildcard match)
        in: query
        name: mailbox
        schema:
          description: Search for a mailbox by email address (case insensitive, wildcard match)
          nullable: true
          type: string
      - description: Search in the Message-ID header (case insensitive wildcard match)
        in: query
        name: message_id
        schema:
          description: Search in the Message-ID header (case insensitive wildcard match)
          nullable: true
          type: string
      - description: The (zero-based) offset of the message groups to return
        in: query
        name: offset
        schema:
          description: The (zero-based) offset of the message groups to return
          format: int32
          nullable: true
          type: integer
      - description: Search in the From field (case insensitive wildcard match)
        in: query
        name: sender
        schema:
          description: Search in the From field (case insensitive wildcard match)
          nullable: true
          type: string
      - description: Search for messages in any of the given states
        explode: true
        in: query
        name: states
        schema:
          description: Search for messages in any of the given states
          items:
            type: string
          type: array
        style: form
      - description: Search in the message subject (case insensitive wildcard match)
        in: query
        name: subject
        schema:
          description: Search in the message subject (case insensitive wildcard match)
          nullable: true
          type: string
      - description: Search in the To, CC, and Bcc fields (case insensitive wildcard match). If possible, use 'mailbox' and 'type' instead for better performance
        in: query
        name: to
        schema:
          description: Search in the To, CC, and Bcc fields (case insensitive wildcard match). If possible, use 'mailbox' and 'type' instead for better performance
          nullable: true
          type: string
      - description: Search for messages by type
        in: query
        name: type
        schema:
          description: Search for messages by type
          nullable: true
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesV0ListMessageGroupsResponse'
          description: OK
      summary: Search message groups
      tags:
      - Message Groups
  /v0/message-groups/siem-summary:
    get:
      description: Returns a paginated, reduced-field summary of flagged message groups intended for SIEM ingestion. Array fields are bounded to keep payloads under typical SIEM batch limits.
      operationId: getMessageGroupSIEMSummary
      parameters:
      - description: Opaque pagination cursor returned in the previous response's cursor field.
        in: query
        name: cursor
        schema:
          description: Opaque pagination cursor returned in the previous response's cursor field.
          nullable: true
          type: string
      - description: The maximum number of message groups to return.
        in: query
        name: limit
        schema:
          default: 100
          description: The maximum number of message groups to return.
          format: int32
          maximum: 1000
          minimum: 1
          type: integer
      - description: Inclusive lower bound on the sort timestamp (newest_created_at for type=flagged, first_reported_as_phish_at for type=reported).
        in: query
        name: timestamp__gte
        schema:
          description: Inclusive lower bound on the sort timestamp (newest_created_at for type=flagged, first_reported_as_phish_at for type=reported).
          format: date-time
          nullable: true
          type: string
      - description: Exclusive upper bound on the sort timestamp (newest_created_at for type=flagged, first_reported_as_phish_at for type=reported).
        in: query
        name: timestamp__lt
        schema:
          description: Exclusive upper bound on the sort timestamp (newest_created_at for type=flagged, first_reported_as_phish_at for type=reported).
          format: date-time
          nullable: true
          type: string
      - description: 'Which timestamp the endpoint sorts/filters on: ''flagged'' uses newest_created_at, ''reported'' uses first_reported_as_phish_at.'
        in: query
        name: type
        schema:
          default: flagged
          description: 'Which timestamp the endpoint sorts/filters on: ''flagged'' uses newest_created_at, ''reported'' uses first_reported_as_phish_at.'
          enum:
          - flagged
          - reported
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesMessageGroupSIEMSummaryResponse'
          description: OK
      summary: Get SIEM-oriented summary of flagged message groups
      tags:
      - Message Groups
  /v0/message-groups/trash:
    post:
      description: Trash all messages in multiple groups, including future messages. For an Office 365 message source, each message will be moved to the "Recoverable Items" folder. For a Google Workspace message source, the "Trash" label will be added to each message.
      operationId: trashMultipleMessageGroups
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrashMultipleMessageGroupsInput'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesTaskAccepted'
          description: Accepted
      summary: Trash multiple message groups
      tags:
      - Message Groups
  /v0/message-groups/{id}:
    get:
      description: Retrieve the details of a single message group.
      operationId: getMessageGroup
      parameters:
      - description: Canonical ID of the message group to retrieve
        in: path
        name: id
        required: true
        schema:
          description: Canonical ID of the message group to retrieve
          type: string
      - description: Sort previews with these message IDs at the top
        explode: true
        in: query
        name: sort_previews_message_id__is
        schema:
          description: Sort previews with these message IDs at the top
          items:
            type: string
          type: array
        style: form
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesMessageGroupV0'
          description: OK
      summary: Get a single message group
      tags:
      - Message Groups
  /v0/message-groups/{id}/dismiss:
    post:
      description: Dismiss all messages in a group, including future messages.
      operationId: dismissMessageCanonicalGroup
      parameters:
      - description: Canonical ID of the message group to dismiss
        in: path
        name: id
        required: true
        schema:
          description: Canonical ID of the message group to dismiss
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DismissMessageCanonicalGroupInput'
      responses:
        '200':
          description: OK
      summary: Dismiss message group
      tags:
      - Message Groups
  /v0/message-groups/{id}/quarantine:
    post:
      description: Quarantine all messages in a group, including future messages.
      operationId: quarantineMessageCanonicalGroup
      parameters:
      - description: Canonical ID of the message group to quarantine
        in: path
        name: id
        required: true
        schema:
          description: Canonical ID of the message group to quarantine
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuarantineMessageCanonicalGroupInput'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesTaskAccepted'
          description: Accepted
      summary: Quarantine message group
      tags:
      - Message Groups
  /v0/message-groups/{id}/restore:
    post:
      description: Restore all previously-trashed or quarantined messages in a group and turn off automatic trashing/quarantining of future messages in the group. "For an Office 365 message source, each message will be put back in the folder it was in before. For a Google Workspace message source, any applied labels (e.g. "Trash") will be removed from each message."
      operationId: restoreMessageCanonicalGroup
      parameters:
      - description: Canonical ID of the message group to restore
        in: path
        name: id
        required: true
        schema:
          description: Canonical ID of the message group to restore
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RestoreMessageCanonicalGroupInput'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesTaskAccepted'
          description: Accepted
      summary: Restore message group
      tags:
      - Message Groups
  /v0/message-groups/{id}/share-with-sublime:
    post:
      description: Share a message with the Sublime team
      operationId: shareWithSublimePublic
      parameters:
      - description: Canonical ID of the message group to share
        in: path
        name: id
        required: true
        schema:
          description: Canonical ID of the message group to share
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ShareWithSublimePublicInput'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesTaskAccepted'
          description: Accepted
      summary: Share a message with Sublime
      tags:
      - Message Groups
  /v0/message-groups/{id}/trash:
    post:
      description: Trash all messages in a group, including future messages. For an Office 365 message source, each message will be moved to the "Recoverable Items" folder. For a Google Workspace message source, the "Trash" label will be added to each message.
      operationId: trashMessageCanonicalGroup
      parameters:
      - description: Canonical ID of the message group to trash
        in: path
        name: id
        required: true
        schema:
          description: Canonical ID of the message group to trash
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrashMessageCanonicalGroupInput'
      responses:
        '202':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Handler_typesTaskAccepted'
          description: Accepted
      summary: Trash message group
      tags:
      - Message Groups
components:
  schemas:
    Handler_typesMessageGroupActivityEvent:
      properties:
        client_ip:
          description: Client IP of the click (click events only)
          nullable: true
          type: string
        forward_recipients:
          $ref: '#/components/schemas/BoundedValues_string'
        mailbox:
          $ref: '#/components/schemas/Handler_typesMessageGroupActivityEventMailbox'
        message_id:
          description: ID of the message, except for 'received' events
          nullable: true
          type: string
        recipients:
          $ref: '#/components/schemas/TruncatedBoundedValues_string'
        timestamp:
          description: Time of the event
          format: date-time
          type: string
        type:
          description: Type of activity event
          type: string
        url:
          description: URL that was clicked (click events only)
          nullable: true
          type: string
        user_agent:
          description: User agent of the click (click events only)
          nullable: true
          type: string
      type: object
    Handler_typesMessageReportV0:
      properties:
        channel:
          description: Channel used to report the group
          type: string
        reported_at:
          description: Time the group was reported
          format: date-time
          type: string
        reported_by_message_id:
          description: ID of the reporting message (the user forward)
          nullable: true
          type: string
        reporter:
          description: Email address of the user who reported the group
          type: string
      type: object
    Handler_typesMessageGroupSIEMSummaryItem:
      properties:
        attachment_hashes:
          $ref: '#/components/schemas/BoundedValues_string'
        attachment_names:
          $ref: '#/components/schemas/BoundedValues_string'
        detection_rule_names:
          description: Names of detection rules that flagged messages in the group
          items:
            type: string
          type: array
        first_seen:
          description: Oldest message effective_at in the group
          format: date-time
          type: string
        id:
          description: Canonical ID of the message group
          type: string
        last_seen:
          description: Newest message effective_at in the group
          format: date-time
          type: string
        link_clicks:
          $ref: '#/components/schemas/TruncatedBoundedValues_handler_types.MessageGroupActivityEvent'
        message_count:
          description: Effective number of messages in the group
          format: int32
          type: integer
        recipient_count:
          description: Number of unique recipients across the group
          format: int32
          type: integer
        sender_domains:
          $ref: '#/components/schemas/BoundedValues_string'
        senders:
          $ref: '#/components/schemas/BoundedValues_string'
        source_ips:
          $ref: '#/components/schemas/TruncatedBoundedValues_string'
        subjects:
          $ref: '#/components/schemas/BoundedValues_string'
      type: object
    Handler_typesMessageGroupActivityEventMailbox:
      description: Details about the mailbox associated with the message, except for 'received' events
      properties:
        display_name:
          description: Display name of the mailbox
          nullable: true
          type: string
        email_address:
          description: Email address of the mailbox
          type: string
        external_id:
          description: External ID of the mailbox
          nullable: true
          type: string
      type: object
    Handler_typesV0ListMessageGroupsResponse:
      properties:
        count:
          description: Count of messages groups for this page
          format: int32
          type: integer
        message_groups:
          description: Array of messages groups matching the search
          items:
            $ref: '#/components/schemas/Handler_typesMessageGroupV0'
          type: array
        stats_limit_exceeded:
          description: Indicates if 'total' is a lower bound
          type: boolean
        total:
          description: Total number of messages groups matching the search
          format: int32
          type: integer
      required:
      - count
      - total
      type: object
    Handler_typesMessageGroupEventLinksClickedV0:
      properties:
        clicks:
          description: Click events for the URL
          items:
            $ref: '#/components/schemas/Handler_typesMessageGroupEventLinkClick'
          type: array
        url:
          description: URL that was clicked
          type: string
      type: object
    RestoreMultipleMessageGroupsInput:
      properties:
        classification:
          description: Classification to apply to the messages being reviewed. Optional.
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          type: string
        message_group_ids:
          description: Canonical IDs of the message groups to restore
          items:
            type: string
          minItems: 1
          type: array
        report_label:
          description: Label to apply to messages being reviewed. Optional.
          enum:
          - spam
          - phishing
          - false_positive
          - false_negative
          - phishing_simulation
          - graymail
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
      required:
      - message_group_ids
      type: object
    Handler_typesMessageGroupV0:
      properties:
        classification:
          description: Classification of the message group
          nullable: true
          type: string
        flagged_rules:
          description: Flagged rules from the message
          items:
            $ref: '#/components/schemas/Handler_typesFlaggedRuleV0'
          type: array
        id:
          description: Canonical ID of the group
          type: string
        message_links_clicked:
          description: Message link clicked events
          items:
            $ref: '#/components/schemas/Handler_typesMessageGroupEventLinksClickedV0'
          type: array
        messages:
          description: Message previews
          items:
            $ref: '#/components/schemas/Handler_typesMessagePreviewV0'
          type: array
        organization_id:
          description: ID of the group's organization
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
        review_label:
          description: Message group label
          nullable: true
          type: string
        review_status:
          description: Message group status
          nullable: true
          type: string
        state:
          description: Message group state
          nullable: true
          type: string
        user_reports:
          description: User reports of the message
          items:
            $ref: '#/components/schemas/Handler_typesMessageReportV0'
          type: array
      required:
      - id
      type: object
    Handler_typesRecipient:
      properties:
        email:
          description: Email address
          type: string
      required:
      - email
      type: object
    Handler_typesSender:
      description: Details of the message sender
      properties:
        display_name:
          description: Display name
          type: string
        email:
          description: Email address
          type: string
      required:
      - email
      type: object
    GraymailMultipleMessageGroupsInput:
      properties:
        classification:
          description: Classification to apply to the messages being reviewed. Optional.
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          type: string
        message_group_ids:
          description: Canonical IDs of the message groups to move to graymail
          items:
            type: string
          minItems: 1
          type: array
        report_label:
          description: Label to apply to messages being reviewed. Optional.
          enum:
          - spam
          - phishing
          - false_positive
          - false_negative
          - phishing_simulation
          - graymail
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
      required:
      - message_group_ids
      type: object
    BoundedValues_string:
      description: Bounded first attachment names on messages in the group
      properties:
        total:
          format: int32
          type: integer
        values:
          items:
            type: string
          type: array
      type: object
    RestoreMessageCanonicalGroupInput:
      properties:
        classification:
          description: Classification to apply to the messages being reviewed. Optional.
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          type: string
        report_label:
          description: Label to apply to messages being reviewed. Optional.
          enum:
          - spam
          - phishing
          - false_positive
          - false_negative
          - phishing_simulation
          - graymail
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
      type: object
    Handler_typesMessageGroupEventLinkClick:
      properties:
        clicked_at:
          description: The time that the link was clicked
          format: date-time
          type: string
        client_ip:
          description: The originating client IP for the click
          type: string
        mailbox_display_name:
          description: Display name of mailbox that performed the action
          nullable: true
          type: string
        mailbox_email_address:
          description: Mailbox email address that performed the action
          nullable: true
          type: string
        mailbox_external_id:
          description: External ID of the mailbox that performed the action
          nullable: true
          type: string
        message_id:
          description: ID of the Sublime message for which the action occurred
          type: string
        user_agent:
          description: The originating user-agent for the click
          type: string
      type: object
    TruncatedBoundedValues_handler_types.MessageGroupActivityEvent:
      description: Link clicks observed on messages in the group, newest first; for full history use GET /v1/messages/groups/{id}/activity?type=click
      properties:
        total:
          format: int32
          type: integer
        truncated:
          type: boolean
        values:
          items:
            $ref: '#/components/schemas/Handler_typesMessageGroupActivityEvent'
          type: array
      type: object
    HuntMessageGroupsInput:
      properties:
        created_at[gte]:
          description: Inclusive start datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-03-14T15:09:26Z'). Only message groups with a message processed at or after this time will be returned.
          format: date-time
          nullable: true
          type: string
        created_at[lt]:
          description: Exclusive end datetime filter for search, in UTC using the ISO 8601 format (e.g., '2021-05-04T15:09:26Z'). Only message groups with a message processed before this time will be returned.
          format: date-time
          nullable: true
          type: string
        private:
          description: Restrict hunt visibility to admin-only
          type: boolean
        source:
          description: Source of MQL to hunt with
          type: string
      required:
      - source
      type: object
    DismissMessageCanonicalGroupInput:
      properties:
        classification:
          description: Classification to apply to the messages being reviewed. Optional.
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          type: string
        report_label:
          description: Label to apply to messages being reviewed. Optional.
          enum:
          - spam
          - phishing
          - false_positive
          - false_negative
          - phishing_simulation
          - graymail
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
      type: object
    TrashMessageCanonicalGroupInput:
      properties:
        classification:
          description: Classification to apply to the messages being reviewed. Optional.
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          type: string
        report_label:
          description: Label to apply to messages being reviewed. Optional.
          enum:
          - spam
          - phishing
          - false_positive
          - false_negative
          - phishing_simulation
          - graymail
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
      type: object
    TruncatedBoundedValues_string:
      description: The first recipient email addresses of the message involved in the event (received events only), may truncate with as few as 1 recipient
      properties:
        total:
          format: int32
          type: integer
        truncated:
          type: boolean
        values:
          items:
            type: string
          type: array
      type: object
    Handler_typesGetHuntResponse:
      properties:
        message_groups:
          description: Array of messages groups returned by the hunt
          items:
            $ref: '#/components/schemas/Handler_typesMessageGroupV0'
          type: array
        task_response:
          $ref: '#/components/schemas/Handler_typesGetTaskResponse'
      type: object
    Handler_typesGetTaskResponse:
      description: Generic information about the scan task execution, such as status or errors.
      properties:
        created_at:
          description: Task creation time
          format: date-time
          type: string
        error:
          description: Task error
          type: string
        id:
          description: Task ID
          format: uuid
          type: string
        state:
          description: Task status
          enum:
          - pending
          - started
          - succeeded
          - failed
          - retrying
          type: string
      required:
      - id
      - state
      type: object
    Handler_typesMessagePreviewV0:
      properties:
        created_at:
          description: Time this message was added to Sublime
          format: date-time
          type: string
        delivered:
          description: Whether or not the message has been delivered
          type: boolean
        forward_recipients:
          description: Email addresses this message was forwarded to by the recipient mailbox
          items:
            type: string
          type: array
        forwarded_at:
          description: Time this message was forwarded by the recipient mailbox. A null value indicates that it has not yet been forwarded
          format: date-time
          nullable: true
          type: string
        id:
          description: Sublime message ID
          type: string
        mailbox:
          $ref: '#/components/schemas/Handler_typesMailbox'
        read_at:
          description: Time this message was read in the user's mailbox. A null value indicates that it has not yet been marked read
          format: date-time
          nullable: true
          type: string
        recipients:
          description: Details of the message recipients
          items:
            $ref: '#/components/schemas/Handler_typesRecipient'
          type: array
        replied_at:
          description: Time that this message was replied to by the recipient mailbox. A null value indicates that it has not yet been replied to by the recipient
          format: date-time
          nullable: true
          type: string
        sender:
          $ref: '#/components/schemas/Handler_typesSender'
        subject:
          description: Subject of the message
          type: string
      required:
      - id
      - mailbox
      - recipients
      - sender
      - subject
      type: object
    Handler_typesMessageGroupSIEMSummaryResponse:
      properties:
        cursor:
          description: Opaque cursor for the next page; pass it back as the cursor query param. Null when no more pages are available.
          nullable: true
          type: string
        message_groups:
          description: List of flagged message groups for SIEM ingestion
          items:
            $ref: '#/components/schemas/Handler_typesMessageGroupSIEMSummaryItem'
          type: array
      type: object
    ShareWithSublimePublicInput:
      properties:
        destination:
          description: Consent-scoped destination on Themis. Empty for the default shared-samples flow.
          type: string
        report_label:
          description: User reported label that categorizes the report
          enum:
          - spam
          - phishing
          - false_positive
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for the report
          type: string
      type: object
    Handler_typesFlaggedRuleV0:
      properties:
        id:
          description: ID of the flagged rule
          type: string
        name:
          description: Name of the flagged rule
          type: string
        tags:
          description: List of tags for the flagged rule
          items:
            type: string
          type: array
      type: object
    DismissMultipleMessageGroupsInput:
      properties:
        classification:
          description: Classification to apply to the messages being reviewed. Optional.
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          type: string
        message_group_ids:
          description: Canonical IDs of the message groups to dismiss
          items:
            type: string
          minItems: 1
          type: array
        report_label:
          description: Label to apply to messages being reviewed. Optional.
          enum:
          - spam
          - phishing
          - false_positive
          - false_negative
          - phishing_simulation
          - graymail
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
      required:
      - message_group_ids
      type: object
    Handler_typesMailbox:
      description: Mailbox that the email bomb was found in
      properties:
        email:
          description: Mailbox email address
          type: string
        external_id:
          description: ID of the mailbox in the source system (e.g., Office 365 or Google Workspace)
          nullable: true
          type: string
        id:
          description: Mailbox ID
          format: uuid
          type: string
      required:
      - email
      - id
      type: object
    TrashMultipleMessageGroupsInput:
      properties:
        classification:
          description: Classification to apply to the messages being reviewed. Optional.
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          type: string
        message_group_ids:
          description: Canonical IDs of the message groups to trash
          items:
            type: string
          minItems: 1
          type: array
        report_label:
          description: Label to apply to messages being reviewed. Optional.
          enum:
          - spam
          - phishing
          - false_positive
          - false_negative
          - phishing_simulation
          - graymail
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
      required:
      - message_group_ids
      type: object
    HandlersReviewMessagesResponse:
      properties:
        actions:
          description: List of actions that were applied
          items:
            type: string
          type: array
        task_id:
          description: Tracking ID of the parent task
          nullable: true
          type: string
      type: object
    QuarantineMessageCanonicalGroupInput:
      properties:
        classification:
          description: Classification to apply to the messages being reviewed. Optional.
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          type: string
        report_label:
          description: Label to apply to messages being reviewed. Optional.
          enum:
          - spam
          - phishing
          - false_positive
          - false_negative
          - phishing_simulation
          - graymail
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
      type: object
    Handler_typesTaskAccepted:
      properties:
        task_id:
          description: Task ID. Use the /v0/tasks/:id endpoint to check the task status.
          format: uuid
          type: string
      required:
      - task_id
      type: object
    ReviewMessageGroupsInput:
      properties:
        action:
          description: Action to take on the message group
          enum:
          - restore
          - warning_banner
          - quarantine
          - trash
          - move_to_spam
          - move_to_graymail
          - delete_calendar_events
          type: string
        classification:
          description: Classification of the message group
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          - skip
          type: string
        custom_action_ids:
          description: IDs of actions to perform on the message group
          items:
            type: string
          type: array
        message_group_ids:
          description: Canonical IDs of the message groups to review
          items:
            type: string
          minItems: 1
          type: array
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
        share_with_sublime:
          description: Whether to share the message group & review with Sublime
          type: boolean
      required:
      - classification
      - message_group_ids
      type: object
    QuarantineMultipleMessageGroupsInput:
      properties:
        classification:
          description: Classification to apply to the messages being reviewed. Optional.
          enum:
          - malicious
          - benign
          - spam
          - graymail
          - simulation
          - unwanted
          - violation
          - non-violation
          type: string
        message_group_ids:
          description: Canonical IDs of the message groups to quarantine
          items:
            type: string
          minItems: 1
          type: array
        report_label:
          description: Label to apply to messages being reviewed. Optional.
          enum:
          - spam
          - phishing
          - false_positive
          - false_negative
          - phishing_simulation
          - graymail
          - violation
          - non-violation
          type: string
        review_comment:
          description: Comment describing reason for action
          nullable: true
          type: string
      required:
      - message_group_ids
      type: object
  securitySchemes:
    bearerAuth:
      scheme: bearer
      type: http
x-readme:
  explorer-enabled: false
